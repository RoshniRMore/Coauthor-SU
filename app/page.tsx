import Link from 'next/link';import {idx} from '../lib/data';
export default function Home(){return <><h1>See what Syracuse researches.</h1><p className="lead">Explore the university’s work as connected themes, then describe an idea to find faculty, groups, and peers—with the evidence behind every match.</p><div className="toolbar"><Link className="button primary" href="/idea">Place your idea on the map</Link><span className="meta">60 derived themes · offline demo corpus</span></div><section><h2>Research landscape</h2><div className="theme-map">{idx.themes.map((t,i)=><Link className="theme" href={`/themes/${t.id}`} key={t.id} style={{minHeight:90+(t.publication_count%4)*18}}><strong>{t.name}</strong><span>{t.faculty_count} faculty · {t.publication_count} works</span></Link>)}</div></section></>}
