'use client';import {useRouter} from 'next/navigation';import {FormEvent} from 'react';
export default function Idea(){const r=useRouter();function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);localStorage.setItem('idea',String(fd.get('idea')));r.push('/matches')}return <><h1>What are you curious about?</h1><p className="lead">Plain language is enough. We’ll first place it in the research landscape, then show evidence-backed matches.</p><form className="form" onSubmit={go}><label><span>Your research idea</span><textarea required name="idea" rows={7} placeholder="I want to study how extreme heat affects older adults in cities…"/></label><label><span>Courses taken</span><input name="courses" placeholder="Statistics, public health, Python"/></label><label><span>Hours per week</span><select name="hours"><option>3–5 hours</option><option>6–10 hours</option><option>10+ hours</option></select></label><label><span>Timeline</span><select name="timeline"><option>This semester</option><option>Next semester</option><option>Summer</option></select></label><button className="primary">Find where this idea fits</button></form></>}
