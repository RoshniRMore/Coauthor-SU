'use client';import {useEffect,useState} from 'react';import students from '../../data/students.json';
export default function Faculty(){const[open,setOpen]=useState(false);useEffect(()=>setOpen(localStorage.getItem('optedIn')==='true'),[]);function toggle(){const n=!open;setOpen(n);localStorage.setItem('optedIn',String(n))}return <><h1>Student ideas near your work.</h1><p className="lead">A read-only demo feed for a faculty profile in climate resilience and public health.</p><div className="notice"><button className={open?'primary':''} onClick={toggle} aria-pressed={open}>{open?'Open to students this semester':'Mark me open to students'}</button><p>{open?'Your explicit availability now contributes to match ranking.':'No availability is inferred from publication or grant activity.'}</p></div><h2>Relevant student posts</h2><div className="grid">{students.slice(0,6).map((s,i)=><article className="card" key={s.id}><span className="meta">{94-i*5}% theme fit</span><h3>{s.idea}</h3><p>{s.name} · {s.courses.join(', ')}</p><p><strong>Why this match:</strong> The post overlaps with your climate and community-health theme.</p></article>)}</div></>}
